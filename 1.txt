    logFile = 'APFDQN_log.txt'  # 训练日志
    fd = open(logFile, 'w')  # 初始化日志
    s = 'episode=%d,reward=%d,reach=%d,step=%d' % (0, 0, 0, 0)
    fd.write(s + '\n')
    fd.close()

##episode=0,reward=0,reach=0,step=0

s = 'episode=%d,reward=%f,reach=%d,step=%d' % (i, episode_reward, env.agent.activity, step)
fd = open(logFile, %'a')
fd.write(s + '\n')  # 写入日志
fd.close()